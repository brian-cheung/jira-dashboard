{
  "name": "jira-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npx vite --port 5173",
    "build": "cd client && npx vite build",
    "build:pages": "cd client && cp index.html index.server.html && cp pages.html index.html && BUILD_PAGES=1 npx vite build --outDir ../docs --emptyOutDir && mv index.server.html index.html",
    "start": "cd server && node index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
